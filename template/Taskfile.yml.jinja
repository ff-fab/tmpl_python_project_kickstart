# https://taskfile.dev
# Cross-platform task runner for {{ module_name }}.
#
# Usage:
#   task                  # List available tasks
#   task test             # Run all tests (example task)
#   task --list           # List available tasks

version: '3'

vars:
  PKG: packages
  MODULE_NAME: '{{ module_name }}'
{% raw %}
  # Guard vars: detect whether test files exist.
  # Used by test tasks to skip gracefully when directories are empty
  # (common in early development). The find runs lazily per task invocation.
  HAS_UNIT_TESTS:
    sh: find {{.PKG}}/tests/unit/ \( -name 'test_*.py' -o -name '*_test.py' \) 2>/dev/null | grep -qm1 . && echo true || echo false
  HAS_INTEGRATION_TESTS:
    sh: find {{.PKG}}/tests/integration/ \( -name 'test_*.py' -o -name '*_test.py' \) 2>/dev/null | grep -qm1 . && echo true || echo false

tasks:
  default:
    desc: List available tasks
    cmds:
      - task --list

  # ===========================================================================
  # Planning Tasks — Beads (bd) issue tracker views
  # ===========================================================================
  # These wrap bd commands so you can think in phase names, not issue IDs.
  # Phase-scoped tasks accept a search term: task plan:phase -- frontend

  plan:
    desc: Show progress per phase (epic completion overview)
    silent: true
    cmds:
      - bash scripts/plan-overview.sh

  plan:ready:
    desc: Show unblocked work you can start right now
    silent: true
    cmds:
      - bd ready

  plan:phase:
    desc: 'Show backlog for a phase (e.g.: task plan:phase -- frontend)'
    silent: true
    cmds:
      - |
        SEARCH="{{.CLI_ARGS}}"
        if [ -z "$SEARCH" ]; then
          EPIC_ID=$(bd query 'type=epic AND status=open' --json 2>/dev/null | jq -r 'sort_by(.priority) | .[0].id // empty')
          if [ -z "$EPIC_ID" ]; then
            echo "No open epics found." >&2; exit 1
          fi
          TITLE=$(bd show "$EPIC_ID" --json 2>/dev/null | jq -r '.[0].title')
          echo "Auto-selected: $TITLE ($EPIC_ID)"
          echo "─────────────────────────────────────────"
        else
          # Try as ID first (exact match or with lh- prefix)
          EPIC_ID=$(bd show "$SEARCH" --json 2>/dev/null | jq -r 'if type == "array" then .[0] | select(.issue_type == "epic") | .id // empty else empty end')
          if [ -z "$EPIC_ID" ]; then
            EPIC_ID=$(bd show "lh-$SEARCH" --json 2>/dev/null | jq -r 'if type == "array" then .[0] | select(.issue_type == "epic") | .id // empty else empty end')
          fi
          # Fall back to title search
          if [ -z "$EPIC_ID" ]; then
            EPIC_ID=$(bd query "type=epic AND title=$SEARCH" --json 2>/dev/null | jq -r '.[0].id // empty')
          fi
          if [ -z "$EPIC_ID" ]; then
            echo "No epic matching '$SEARCH'. Available epics:" >&2
            bd list --type epic --all --limit 0 >&2; exit 1
          fi
        fi
        bd children "$EPIC_ID"

  plan:order:
    desc: 'Show execution order within a phase (e.g.: task plan:order -- frontend)'
    silent: true
    cmds:
      - |
        SEARCH="{{.CLI_ARGS}}"
        if [ -z "$SEARCH" ]; then
          EPIC_ID=$(bd query 'type=epic AND status=open' --json 2>/dev/null | jq -r 'sort_by(.priority) | .[0].id // empty')
          if [ -z "$EPIC_ID" ]; then
            echo "No open epics found." >&2; exit 1
          fi
          TITLE=$(bd show "$EPIC_ID" --json 2>/dev/null | jq -r '.[0].title')
          echo "Auto-selected: $TITLE ($EPIC_ID)"
          echo "─────────────────────────────────────────"
        else
          # Try as ID first (exact match or with lh- prefix)
          EPIC_ID=$(bd show "$SEARCH" --json 2>/dev/null | jq -r 'if type == "array" then .[0] | select(.issue_type == "epic") | .id // empty else empty end')
          if [ -z "$EPIC_ID" ]; then
            EPIC_ID=$(bd show "lh-$SEARCH" --json 2>/dev/null | jq -r 'if type == "array" then .[0] | select(.issue_type == "epic") | .id // empty else empty end')
          fi
          # Fall back to title search
          if [ -z "$EPIC_ID" ]; then
            EPIC_ID=$(bd query "type=epic AND title=$SEARCH" --json 2>/dev/null | jq -r '.[0].id // empty')
          fi
          if [ -z "$EPIC_ID" ]; then
            echo "No epic matching '$SEARCH'. Available epics:" >&2
            bd list --type epic --all --limit 0 >&2; exit 1
          fi
        fi
        bd graph "$EPIC_ID" --compact

  plan:ui:
    desc: Interactive epic/task browser with drill-down (requires fzf)
    silent: true
    cmds:
      - bash scripts/plan-interactive.sh

  plan:task:
    desc: 'Show details of a task (e.g.: task plan:task ID=lh-6yy.3)'
    silent: true
    vars:
      ID: ''
    cmds:
      - |
        SEARCH="{{.ID}}"
        if [ -z "$SEARCH" ]; then SEARCH="{{.CLI_ARGS}}"; fi
        if [ -z "$SEARCH" ]; then
          echo "Usage: task plan:task ID=<task-id-or-title>" >&2
          echo "   or: task plan:task -- <task-id-or-title>" >&2
          exit 1
        fi
        # Try as ID first (exact match or with lh- prefix)
        TASK_ID=$(bd show "$SEARCH" --json 2>/dev/null | jq -r 'if type == "array" then .[0] | select(.issue_type != "epic") | .id // empty else empty end')
        if [ -z "$TASK_ID" ]; then
          TASK_ID=$(bd show "lh-$SEARCH" --json 2>/dev/null | jq -r 'if type == "array" then .[0] | select(.issue_type != "epic") | .id // empty else empty end')
        fi
        # Fall back to title search
        if [ -z "$TASK_ID" ]; then
          TASK_ID=$(bd query "type!=epic AND title=$SEARCH" --json 2>/dev/null | jq -r '.[0].id // empty')
        fi
        if [ -z "$TASK_ID" ]; then
          echo "No task matching '$SEARCH'." >&2; exit 1
        fi
        bd show "$TASK_ID"

  # ===========================================================================
  # Beads Sync — Export Dolt DB state to git-tracked JSONL
  # ===========================================================================
  # bd close updates the local Dolt DB but does NOT update .beads/issues.jsonl.
  # Always run this task after closing beads tasks and before committing.

  beads:sync:
    desc: Export beads DB to .beads/issues.jsonl (run after bd close)
    silent: true
    preconditions:
      - sh: command -v bd >/dev/null 2>&1
        msg: bd is not installed
      - sh: test -d .beads
        msg: .beads/ directory not found
    cmds:
      - bd export -o .beads/issues.jsonl
      - echo "✓ Exported beads state to .beads/issues.jsonl"


  # ===========================================================================
  # Test Tasks - Top Level
  # ===========================================================================

  test:
    desc: Run all tests (unit + integration) with coverage and summary
    cmds:
      # Run all tests with coverage to enable threshold validation.
      - cmd: |
          if [ "{{.HAS_UNIT_TESTS}}" != "true" ]; then
            echo "ℹ No unit tests found — skipping"
            exit 0
          fi
          uv run pytest {{.PKG}}/tests/unit/ -v --tb=short --junitxml=results-unit.xml --cov={{.MODULE_NAME}} --cov-branch --cov-report=json
        ignore_error: true
{% endraw %}
{% if robot_framework %}
{% raw %}
      - task: test:integration
{% endraw %}
{% endif %}
{% raw %}
      - |
        # Only run the summary when pytest actually produced results.
        if [ -f results-unit.xml ]; then
          uv run python {{.PKG}}/tests/scripts/summarize_tests.py --coverage-file=coverage.json
        else
          echo "ℹ No test results — skipping summary"
        fi

  test:unit:
    desc: Run unit tests only (fast, every change)
    cmds:
      - |
        if [ "{{.HAS_UNIT_TESTS}}" != "true" ]; then
          echo "ℹ No unit tests found — skipping"
          exit 0
        fi
        uv run pytest {{.PKG}}/tests/unit/ -v --tb=short --junitxml=results-unit.xml

  test:file:
    desc: 'Run specific test file or pattern (e.g.: task test:file -- packages/tests/unit/test_errors.py)'
    cmds:
      - |
        if [ -z "{{.CLI_ARGS}}" ]; then
          echo "Usage: task test:file -- packages/tests/unit/test_foo.py" >&2
          echo "   or: task test:file -- -k test_my_function" >&2
          exit 1
        fi
        uv run pytest {{.CLI_ARGS}} -v --tb=short

{% endraw %}
{% if robot_framework %}
{% raw %}
  test:integration:
    desc: Run integration tests (Robot Framework)
    cmds:
      - uv run robot {{.PKG}}/tests/integration/

  test:file:robot:
    desc: 'Run specific Robot Framework file (e.g.: task test:file:robot -- packages/tests/integration/login.robot)'
    cmds:
      - |
        if [ -z "{{.CLI_ARGS}}" ]; then
          echo "Usage: task test:file:robot -- packages/tests/integration/login.robot" >&2
          exit 1
        fi
        uv run robot {{.CLI_ARGS}}
{% endraw %}
{% else %}
{% raw %}
  test:integration:
    desc: Run integration tests (pytest)
    cmds:
      - |
        if [ "{{.HAS_INTEGRATION_TESTS}}" != "true" ]; then
          echo "ℹ No integration tests found — skipping"
          exit 0
        fi
        uv run pytest {{.PKG}}/tests/integration/ -v --tb=short -m integration
{% endraw %}
{% endif %}
{% raw %}

  test:cov:
    desc: Run tests with coverage (terminal + XML for IDE)
    cmds:
      - uv run pytest {{.PKG}}/tests/ --cov={{.MODULE_NAME}} --cov-branch --cov-report=term-missing --cov-report=xml

  test:cov:html:
    desc: Run tests with HTML coverage report
    cmds:
      - uv run pytest {{.PKG}}/tests/ --cov={{.MODULE_NAME}} --cov-branch --cov-report=html --cov-report=term
      - cmd: '{{if eq OS "darwin"}}open{{else}}xdg-open{{end}} htmlcov/index.html'
        ignore_error: true

  test:watch:
    desc: Run unit tests in watch mode
    cmds:
      - uv run pytest-watch {{.PKG}}/tests/unit/ -- -v --tb=short

  # ===========================================================================
  # Lint & Typecheck
  # ===========================================================================

  lint:
    desc: Run linting (ruff check + format check)
    cmds:
      - uv run ruff check {{.PKG}}/src/ {{.PKG}}/tests/
      - uv run ruff format --check {{.PKG}}/src/ {{.PKG}}/tests/

  lint:fix:
    desc: Fix linting issues automatically
    cmds:
      - uv run ruff check --fix {{.PKG}}/src/ {{.PKG}}/tests/
      - uv run ruff format {{.PKG}}/src/ {{.PKG}}/tests/

  typecheck:
    desc: Run type checking (ty)
    cmds:
      - uv run ty check {{.PKG}}/src/

  check:
    desc: Run all checks (lint + typecheck + test)
    cmds:
      - task: lint
      - task: typecheck
      - task: test

  quality:summary:
    desc: Run lint, typecheck, and unit tests, then output a JSON summary
    cmds:
      - bash scripts/quality-summary.sh

  pre-pr:
    desc: Run all pre-PR quality gates (pre-commit + lint + typecheck + tests + coverage + complexity + similarity)
    cmds:
      - pre-commit run --all-files
      - task: lint
      - task: typecheck
      - task: test
      - task: complexity
      - task: similarity

  # ===========================================================================
  # Documentation
  # ===========================================================================

  docs:serve:
    desc: Preview documentation site with hot reload (port 8001)
    cmds:
      - uv run --group docs zensical serve --dev-addr 0.0.0.0:8001

  docs:build:
    desc: Build documentation site (clean output)
    cmds:
      - uv run --group docs zensical build --clean

  adr:create:
    desc: Create or amend an ADR from a validated JSON input file
    cmds:
      - uv run scripts/render_adr.py {{.CLI_ARGS}}

  # ===========================================================================
  # Dependency Management
  # ===========================================================================

  sync:
    desc: Sync dependencies
    cmds:
      - uv sync --group dev

  # ===========================================================================
  # Update Tasks
  # ===========================================================================

  update:template:
    desc: Update project from copier template (pulls latest template changes)
    cmds:
      - uvx --python 3.14 --with jinja2-time copier update --trust

  update:deps:
    desc: Update all dependencies to latest compatible versions
    cmds:
      - uv lock --upgrade
      - uv sync --all-groups

  # ===========================================================================
  # Code Quality
  # ===========================================================================

  complexity:
    desc: Check all code complexity gates (cyclomatic + cognitive + duplication/similarity)
    cmds:
      - task: complexity:cyclomatic
      - task: complexity:cognitive
      - task: similarity

  complexity:cyclomatic:
    desc: Check cyclomatic complexity (radon + xenon)
    cmds:
      - uv run radon cc {{.PKG}}/src/{{.MODULE_NAME}} --average --show-complexity
      - uv run xenon {{.PKG}}/src/{{.MODULE_NAME}} --max-absolute B --max-modules A --max-average A

  complexity:cognitive:
    desc: Check cognitive complexity (flake8-cognitive-complexity, max 15)
    cmds:
      - uv run flake8 --select=CCR001 {{.PKG}}/src/{{.MODULE_NAME}}/

  similarity:
    desc: Check for code duplication (symilar — ships with pylint)
    cmds:
      - |
        OUTPUT=$(find {{.PKG}}/src/{{.MODULE_NAME}} -name '*.py' -print0 \
          | xargs -0 -x uv run symilar -d 4 --ignore-imports --ignore-signatures)
        echo "$OUTPUT"
        DUPES=$(printf '%s\n' "$OUTPUT" | sed -n 's/.*duplicates=\([0-9][0-9]*\).*/\1/p' | head -n1)
        if [ "${DUPES:-0}" -gt 0 ]; then
          echo "❌ Found $DUPES duplicate lines"
          exit 1
        fi
{% endraw %}
{% if include_sbom %}
{% raw %}
  # ===========================================================================
  # Supply Chain
  # ===========================================================================

  sbom:
    desc: Generate CycloneDX SBOM from built wheel
    cmds:
      - rm -rf dist/
      - uv run --with build python -m build
      - syft dist/*.whl -o cyclonedx-json=dist/{{.MODULE_NAME}}.cdx.json
      - echo "SBOM written to dist/{{.MODULE_NAME}}.cdx.json"
{% endraw %}
{% endif %}
{% raw %}
  # ===========================================================================
  # PR Helpers
  # ===========================================================================

  pr:list:
    desc: 'List open PRs excluding release-please (e.g.: task pr:list)'
    cmds:
      - |
        gh pr list --state open --json number,title,author,headRefName \
          --jq '.[] | select(
            (.author.login | test("please-release|release-please") | not) and
            (.author.login != "app/github-actions" or (.headRefName | test("release-please|please-release") | not))
          )'

  pr:create:
    desc: 'Create a PR (e.g.: task pr:create -- --title "feat: ..." --body "## Summary ...")'
    preconditions:
      - sh: '[ -n "{{.CLI_ARGS}}" ]'
        msg: 'Usage: task pr:create -- --title "feat: title" --body "## Summary ..."'
    cmds:
      - gh pr create {{.CLI_ARGS}}

  pr:diff:
    desc: 'Show PR diff (e.g.: task pr:diff -- 42, or task pr:diff -- 42 --name-only)'
    preconditions:
      - sh: '[ -n "{{.CLI_ARGS}}" ]'
        msg: 'Usage: task pr:diff -- <pr-number> [--name-only]'
    cmds:
      - gh pr diff {{.CLI_ARGS}}

  pr:feedback:
    desc: 'Fetch all PR feedback as JSON (e.g.: task pr:feedback -- 42, or task pr:feedback to auto-detect from current branch)'
    cmds:
      - bash .github/skills/pr-review/fetch-pr-feedback.sh {{.CLI_ARGS}}

  # ===========================================================================
  # CI Helpers
  # ===========================================================================

  ci:wait:
    desc: 'Wait for CI checks to pass on a PR (e.g.: task ci:wait -- 5)'
    cmds:
      - bash scripts/ci-wait.sh {{.CLI_ARGS}}

  # ===========================================================================
  # CI Tasks (used in GitHub Actions)
  # ===========================================================================

  ci:test:unit:
    desc: CI unit tests with coverage summary
    cmds:
      - |
        if [ "{{.HAS_UNIT_TESTS}}" != "true" ]; then
          echo "ℹ No unit tests found in {{.PKG}}/tests/unit/ — skipping"
          exit 0
        fi
        uv run pytest {{.PKG}}/tests/unit/ --cov={{.MODULE_NAME}} --cov-branch --cov-report=xml --cov-report=json --cov-report=term --junitxml=results-unit.xml
        uv run python {{.PKG}}/tests/scripts/summarize_tests.py --coverage-file=coverage.json --fail-under={% endraw %}{{ coverage_threshold }}{% if robot_framework %} --unit-only{% endif %}{% raw %}

{% endraw %}
{% if robot_framework %}
{% raw %}
  ci:test:integration:
    desc: CI integration tests (Robot Framework)
    cmds:
      - uv run robot --outputdir . --xunit results-integration.xml {{.PKG}}/tests/integration/
{% endraw %}
{% else %}
{% raw %}
  ci:test:integration:
    desc: CI integration tests (pytest)
    cmds:
      - |
        if [ "{{.HAS_INTEGRATION_TESTS}}" != "true" ]; then
          echo "ℹ No integration tests found — skipping"
          exit 0
        fi
        uv run pytest {{.PKG}}/tests/integration/ -v --tb=short -m integration --junitxml=results-integration.xml
{% endraw %}
{% endif %}
{% raw %}

  ci:lint:
    desc: CI lint task (strict)
    cmds:
      - uv run ruff check {{.PKG}}/src/ {{.PKG}}/tests/
      - uv run ruff format --check {{.PKG}}/src/ {{.PKG}}/tests/
      - uv run ty check {{.PKG}}/src/
{% endraw %}
